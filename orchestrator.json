{
  "orchestrators": [
    {
      "key": "discipline_classifier",
      "name": "Discipline Classifier Flow",
      "execution_mode": "real",
      "description": "Classify academic papers into 1-23 disciplines (2-phase pipeline: Parse, Classify)",
      "agents": [
        {
          "agent_id": "discipline_classifier",
          "dependencies": [],
          "input_mapping": {
            "file_id": "${input.file_id}",
            "manuscript_id": "${input.manuscript_id}",
            "structured_content_overview_id": "${input.structured_content_overview_id}"
          }
        }
      ]
    }
  ],
  "default_orchestrator": "discipline_classifier",
  "input_schema": {
    "file_id": {
      "type": "string",
      "description": "PDF file ID of the research paper",
      "required": true
    },
    "manuscript_id": {
      "type": "string",
      "description": "Manuscript ID for tracking context",
      "required": false
    },
    "structured_content_overview_id": {
      "type": "string",
      "description": "Already parsed content ID (optional, skips PDF parsing)",
      "required": false
    }
  }
}
